general:
  branches:
    ignore:
      - gh-pages

machine:
  timezone: Asia/Tokyo
  ruby:
    version: 2.2.1
  node:
    version: 4.1.0

dependencies:
  override:
    - rake init_wercker

compile:
  override:
    - rake init_contentful
    - rake build

deployment:
  gh-pages:
    branch: master
    commands:
      - rake deploy
