box: s6464/seisafesta2015
build:
  steps:
    - script:
      name: init
      code: bash -lc 'rake init_wercker'
    - script:
      name: contentful
      code: bash -lc 'rake init_contentful'
    - script:
      name: build
      code: bash -lc 'rake build'
